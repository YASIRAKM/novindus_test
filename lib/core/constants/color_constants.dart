import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF006837);
  static const Color primaryLight = Color.fromARGB(255, 5, 158, 5);
  static const Color background = Color.fromARGB(255, 255, 255, 255);
  static const Color text = Colors.black87;
  static const Color textLight = Colors.grey;
  static const Color white = Colors.white;
  static const Color error = Colors.red;
  static const Color red = Colors.red;
  static const Color border = Color.fromARGB(255, 194, 193, 193);

  static const Color button = primary;
  static const Color textFieldFill = Color(0xFFF1F1F1);
  static const Color icon = textLight;
  static const Color link = Color.fromARGB(255, 4, 0, 255);
}
